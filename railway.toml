[build]
builder = "nixpacks"

[start]
cmd = "mkdir -p storage/framework/sessions storage/framework/views storage/framework/cache && php artisan migrate --force --no-interaction && php artisan serve --host 0.0.0.0 --port $PORT"
