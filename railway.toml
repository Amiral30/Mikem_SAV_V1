[build]
builder = "nixpacks"

[phases.setup]
nixPkgs = ["php82", "php82Extensions.gd", "php82Extensions.zip", "php82Extensions.pdo_mysql"]

[phases.prestart]
cmds = ["php artisan migrate --force", "php artisan db:seed --force"]
